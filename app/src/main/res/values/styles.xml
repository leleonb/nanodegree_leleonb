<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="PortfolioButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">#607D8B</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:layout_marginTop">@dimen/portfolio_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/portfolio_vertical_margin</item>
        <item name="android:layout_marginLeft">@dimen/portfolio_horizontal_margin</item>
        <item name="android:layout_marginRight">@dimen/portfolio_horizontal_margin</item>
    </style>
</resources>
